{
  "name": "spinonediv",
  "version": "0.0.1",
  "description": "A collection of single element css loaders and spinners",
  "homepage": "https://github.com/grammka/spinonediv#readme",
  "author": "Pavel Ivanov (grammka@gmail.com)",
  "license": "MIT",
  "scripts": {
    "clean": "rimraf build",
    "html": "jade ./src -o ./build -P -w",
    "stylus": "stylus ./src/style.styl -o ./build -w",
    "build": "npm run clean && jade ./src -o ./build -P && stylus ./src -o ./build"
  },
  "dependencies": {},
  "devDependencies": {
    "stylus": "^0.54.5"
  }
}
